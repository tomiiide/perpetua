{
  "name": "perpetua",
  "private": true,
  "version": "0.0.0",
  "description": "Foundational widgets for pro trading software",
  "author": {
    "email": "tomideoladipo@gmail.com",
    "name": "tomiiide"
  },
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "knip": "knip",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "knip": "^6.25.0"
  }
}
